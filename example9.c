#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int sat,sut,i,j,n,x,a[i][j];
	if(n%sat==0 && n%sut==0){
		x=0;
		for(i=0;i<sat;i++)
		  for(j=0;j<sut;j++)
		    a[i][j];
		    x++;
	    }
    }
    printf(\n\n"MATRIS>\n");
    for(i=0;i<sat;i++){
    	for(j=0;j<sut;j++)
    	 printf("%3d",a[i][j]);
        printf("\n");
    }
	}else
	   printf("hata");
	
	return 0;
}
